export const dynamic = "force-dynamic";

import RepositoryDashboard from "@/features/repositories/components/RepositoryDashboard";

export default function ImportedRepositoriesPage() {
  return <RepositoryDashboard />;
}